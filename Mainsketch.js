function setup () {
    createCanvas(windowWidth, windowHeight);
    Startscreen();
     boxWidth = windowWidth * 0.03;

    print(thisScreen)
    // print(boxWidth)
}

function draw() {
    Startscreen();
    // drawGridLines()
    arena()
}