function setup () {
    createCanvas(windowWidth, windowHeight);
    Startscreen();

    // let Boundries = [
    //     //bottom
    //     new Grid(windowWidth/2, windowHeight , windowWidth, 50),
    //     //top
    //     new Grid(windowWidth/2, 0 , windowWidth, 50),
    //     //left
    //     new Grid(0, windowHeight/2 , 50, windowHeight),
    //     //right
    //     new Grid(windowWidth, windowHeight/2 , 50, windowHeight)
    // ]

    // let Grids = [
    //     new Grid(windowWidth/2, windowHeight/2, barrierWidth, windowHeight),
    // ]
    
    // for (let i = 0; i < Boundries.length; i++) {
    //     Boundries[i].draw();
    // }

    // for (let i = 0; i < Grids.length; i++) {
    //     Grids[i].draw();
    // }

    // circle(200, 200, 50);
}

function draw() {
}